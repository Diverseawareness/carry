# 1.1.2 Hotfix — Progress Log

**Started:** 2026-05-29 · **Branch:** `hotfix/1.1.2` (cut from `feature/profile-v2`)
**Why:** Two critical bugs hit during a real round (Frank's Payday, Ruby Hill, 2 scorer devices).
**Resume rule:** if this session is interrupted, read this file top-to-bottom first.

---

## STATUS AT A GLANCE

| Bug | Root cause found? | Fix written? | Builds? | Committed? |
|---|---|---|---|---|
| **Bug 2 — clipped Save buttons** | ✅ yes | ✅ yes (2 edits) | ✅ BUILD SUCCEEDED | ❌ not yet |
| **Bug 1 — duplicate guests** | ✅ yes + full surface mapped + **financial impact confirmed** | ⏳ BLOCKED on scope choice (A vs B) | not started | — |

**Bug 2 is DONE** — screenshot-proven (iPhone SE render, OLD clips / NEW shows both buttons),
sibling sweep found **zero** other clip-risk sheets (every other `.clipped()`/fractional
height is a wheel picker, decorative crop, or already-adaptive). Class closed with one fix.

**Bug 1 is NOT cosmetic — it's a wrong-money bug.** Full sweep complete (3 parallel audits).
**Immediate next step: Daniel picks fix scope (see "BUG 1 — fix scope decision" below).**
The live-data SQL diagnostic is now optional (we understand the mechanism; the cross-device
case is confirmed by code, not needed to start the fix).

---

## SWEEP RESULTS (2026-05-30) — "are there more bugs?"

### Bug 2 class — CLOSED
No siblings. Audited every `UIScreen.*.bounds.height *`, every `.clipped()`, every custom
bottom sheet. All safe or already-fixed. Nothing else to do.

### Bug 1 is a FINANCIAL bug (severity upgrade)
Skins is real money. The duplicate inflates the pot and can pay a phantom:
- `pot = config.buyIn * allPlayers.count` (`RoundViewModel.swift:208`) → **+1 buy-in per dupe.**
- `skinValue = pot / totalSkins` (`RoundViewModel.swift:241,292`) → inflated numerator →
  gross payouts sum to **more than was collected**; `totalPaid == pot` invariant breaks.
- Dup has its own `stableId` + its own `scores[]` column → **phantom can win holes and be
  owed money** at settlement (`RoundCompleteView.swift:931-946`).
- **No dedup anywhere before the `round_players` insert** (`RoundCoordinatorView.swift:585-593`)
  — DB `UNIQUE(round_id,player_id)` only catches the *same-UUID* case. The **cross-device case
  (two scorer devices each add "Kyle" → two different UUIDs) both insert** = the exact scenario
  Daniel hit. Persists across reloads via `guest_roster_json`.

### Root cause (confirmed)
`Player.id` (Int) has two sources for one guest:
- **A — local counter** `nextGuestID` (100,101…), profileId nil — `addGuest`, offline PlayerGroupsSheet, SMS stubs.
- **B — `stableId(profileId)`** UUID-hash — every server load-back (`Player(from:ProfileDTO)`).
Same human = two ids; every dedup keys on the Int id → both survive. Real Carry users are
immune (always Path B). Cross-device, two UUIDs = two Path-B ids for one human → also dupes.

### Full fix surface (from the identity-map audit)
1. **Kill `nextGuestID` entirely.** Every guest born with a UUID + `id = stableId(uuid)`:
   `GroupManagerView.swift:50,4905-4917,5000-5010` · `GroupsListView.swift:3128,3848-3860` ·
   `PlayerGroupsSheet.swift:85,1113-1126,1306-1318` · `ManageMembersSheet.swift:36,740-746`
   (+ plumbing: PGS:45,340,1374,1694 · MMS:25,94,102,108,147,748 · GMV:380,395,2425,2454).
2. **Add missing id-remap after a guest gains a UUID:** `RoundCoordinatorView.swift:564-570`
   (sets profileId but NOT id — primary offender); after `reservePhoneInvite` in
   `GroupsListView.swift:800-808` + `QuickStartSheet` (mirror the correct remap already at
   `PlayerGroupsSheet.swift:1550-1588`).
3. **Switch every Int-id dedup to a canonical key (`profileId ?? inviteMemberId` else id):**
   `GroupManagerView.swift:364-365,804,968,982,993,1036,1044-1046,1373-1389,4351,4363-4364` ·
   `PlayerGroupsSheet.swift:1650-1678` · `QuickGameGuestStorage.swift:154-155` (**durable
   amplifier — the thing that makes dupes permanent**) · `GroupService.swift:1361,1368` ·
   `RoundCoordinatorView.swift:227,519` · `ManageMembersSheet.swift:81-88`.
4. **Money safety net (last line of defense):** dedup-by-profileId before building
   `playerTuples` in `RoundCoordinatorView.createSupabaseRound` (~:585-593).

### Sibling bugs found (same class, will fix as part of the canonical-identity work)
- **S1** — 3 SMS-invite stubs create pending players with NO `inviteMemberId` → can't reconcile
  with server copy (`GMV:4905`, `GroupsListView:3848`, `PGS:1113`).
- **S2** — scorer id-remap after phone-dedup only done in ONE path (`PGS:1550-1588`); missing in
  `RoundCoordinatorView:562` + `GroupsListView:800-808` → scorer slot wiped on refresh.
- **S3** — `scorerIDs:[Int]` mixed provenance; eligibility resolves wrong when a guest id diverges.
- **S4** — `QuickGameGuestStorage` persists whatever id it's handed → `guest_roster_json` can hold
  BOTH representations = the durable amplifier (turns transient dupe into permanent/cross-device).
- **S5** — `addGuest` (`GMV:5000-5010`) **forgets `isGuest:true`** → guest silently dropped from
  the snapshot filter; compounds the dupe.
- **S6** — `recentlyRemovedIds:Set<Int>` (`GMV:190,913,1033`) → removed guest resurrects under its
  other id. **This is the open MEMORY TODO "removing last player doesn't persist."**

### Decision needed — Bug 1 fix scope for 1.1.2  ⏳ BLOCKED (waiting on Daniel)
Explanation of the bug + the two options was delivered in plain language on 2026-05-30.
No code written yet — fix scope must be chosen first. The guest subsystem is the app's most
fragile (locked invariants); full canonical-identity refactor = ~40 sites / 5 files.
- **A) Money-critical now, rest in 1.1.3 (RECOMMENDED)** — 1.1.2 = items #2 (RoundCoord id-remap),
  #4 (dedup-by-profileId before round_players insert), S4 (snapshot dedup-by-profileId), S5
  (isGuest flag). Closes wrong-money + permanence path. Broad dedup sweep (#3) + S1/S2/S3/S6 → 1.1.3.
  Smaller blast radius for a hotfix; still structural, not a patch.
- **B) Full canonical-identity fix in 1.1.2** — kill `nextGuestID`, profileId-derived stableId
  everywhere, all ~40 sites + all 6 siblings in one release. Cleanest end state, largest blast
  radius vs locked invariants, most testing before it can ship.

**DECISION: Daniel chose B (2026-05-30) — full canonical-identity fix in 1.1.2.**

---

## BUG 1 FIX — canonical-identity design (Option B) + stage tracker

### Why the id splits (confirmed against foundation code)
`create_guest_profiles` (`20260328000000_guest_profiles.sql`) does `new_id := gen_random_uuid()`
SERVER-side — the client cannot supply the UUID. So a guest's `Player.id`
(`= stableId(from: uuid)`) **necessarily changes** at the local→server transition:
`stableId(localUUID)` → `stableId(serverUUID)`. Two devices mint two server UUIDs for one
human → two ids. Any dedup on the raw Int id lets both through.

### Canonical model (the fix)
1. **One key for all dedup/membership/merge:** `Player.canonicalKey`
   = `profileId` if set, else `inviteMemberId` if set, else the local int id.
   (QuickStart already derives ids from UUIDs correctly — this generalizes it.)
2. **All guest births UUID-derived, never a counter.** Eliminate `nextGuestID`.
3. **Heal the id at the server transition:** when a guest gains a `profileId`, set
   `id = stableId(profileId)` AND rewrite the snapshot (so old id is replaced, not doubled).
4. **Snapshot canonicalization:** `GuestSnapshot.asPlayer` derives id from profileId when
   present; `save()` dedups by canonicalKey. Heals already-poisoned `guest_roster_json`.
5. **Cross-device same-name collapse — DEFERRED to a separate decision (the judgment call).**
   `guestHumanKey` (name+handicap) collapse would fix the cross-device case (two devices each
   mint a server UUID for one human → two profileIds → canonicalKey can't collapse them). BUT
   it risks merging two LEGITIMATELY different guests who share name+handicap (e.g. two "Mike"s,
   both handicap 0.0 default) → under-counts the pot = wrong money the OTHER direction. So it is
   NOT auto-applied. Rationale it's likely unnecessary for Daniel's actual bug: the save-path
   deduped by Int id (S4), so the within-device divergence (one guest as both local-id AND
   server-id) almost certainly produced the dupes he saw — which canonicalKey + remap fixes
   cleanly and safely. Cross-device name-collapse will be offered as an explicit opt-in after
   the safe fix lands. `guestHumanKey` stays in Player.swift unused-by-default for that purpose.
6. **Money safety net:** dedup-by-profileId before building `playerTuples` in
   `RoundCoordinatorView.createSupabaseRound`.

### Invariant impact (guest-lifecycle locked rules — all must still hold)
| Invariant | Touched? | How preserved |
|---|---|---|
| Guests in `round_players` only, never `group_members` | No | identity-only change; no table writes added |
| Round-start reconciliation recreates missing profiles | Yes (improve) | ADD id-remap after profileId assigned (step 3); names still from snapshot |
| Disease string "Guest"+0.0 defenses | No | keep all 4 filters; canonicalKey change is orthogonal |
| 4-layer guest-edit persistence chain | Yes (careful) | dedup-by-canonicalKey must not drop the edited copy; reconcile by profileId |
| Games-tab pills 1:1 with roster | Yes (improve) | collapsing dupes makes pills correct, not lossy |

### STAGE TRACKER (each stage builds green before the next)
- [x] **S1 Foundation** — `Player.canonicalKey` + `guestHumanKey` + `guestId(from:)` in Player.swift. ✅ BUILD SUCCEEDED.
- [x] **S2 Birth sites** — DONE, ✅ BUILD SUCCEEDED:
   - [x] `GroupManagerView.addGuest` → UUID id + `isGuest:true` (S5).
   - [x] `PlayerGroupsSheet` offline branch → UUID id; online branch color-idx de-counter'd.
   - [x] 3 SMS stubs: `GMV.sendInvite` (UUID id, server flow untouched), `GroupsListView.sendInlineInvite`
     (UUID id + inviteMemberId anchored), `PGS.sendSlotInvite` (UUID id + inviteMemberId — also fixes
     the scorer-reserve skip). All `nextGuestID += 1` increments removed from these sites.
   - NOTE: SMS server-call semantics deliberately untouched (MEMORY: SMS-scorer reconciliation
     is an active paused investigation). Only identity (id + inviteMemberId) canonicalized.
- [ ] **S3 Dedup sites** — switch ~the Int-id dedups in GMV/PGS/GroupService/RoundCoord/MMS to canonicalKey.
- [ ] **S4 Snapshot + money net** — GuestSnapshot canonicalization + save dedup-by-key + heal; dedup-by-profileId before round_players insert.
- [ ] **S5 Siblings + cleanup** — RoundCoord id-remap (S2), reservePhoneInvite remap, recentlyRemovedIds→key set (S6), remove `nextGuestID` plumbing.
- [ ] **S6 Verify** — build + targeted tests + 2-device guest mental walk-through vs invariants.

**Bug 2 still uncommitted on this branch** — will commit both bugs together (or on request).

### TOOLING NOTE (2026-05-30)
This session's tool output is intermittently corrupting (garbled reads/greps, wrong
edit-path echoes, false PIPESTATUS). Mitigation in use: rely on `xcodebuild` BUILD SUCCEEDED
as the authoritative check (Edit exact-matches and fails safe, so no silent corruption), and
re-read to confirm ground truth. This slows fragile edits — proceeding stage-by-stage with a
build after each rather than batching, to keep the guest subsystem safe.

---

## DID RECENT WORK LEAK TO PRODUCTION? — NO

| Line | Branch | Build | State |
|---|---|---|---|
| **Live App Store** | `release/1.1.0` (tip 2026-05-20) | 85 | pushed to origin |
| **In review (App Review now)** | `feature/profile-v2` (tip 2026-05-27) | 87 (v1.1.1) | local |
| **Junk dual-dial work** | `feature/junk-dots` (tip 2026-05-28) | — | **local-only, never pushed** |

- `git diff release/1.1.0 feature/profile-v2 -- <buggy files>` = **0 lines** → the 4 buggy
  files are byte-identical across 1.1.0, 1.1.1, and pre-edit tree.
- Both bugs confirmed present in the live 1.1.0 binary (grep counts on `git show release/1.1.0:…`).
- **Conclusion:** pre-existing structural flaws, NOT leakage from junk work.

**Junk WIP is parked safely in `stash@{0}`** ("junk-dots WIP: dial+scorecard+confetti+doc
(pre-1.1.2-hotfix)"). 6 files. Do NOT lose this — it's the entire dual-dial session.

---

## BUG 2 — Score sheet Save/Score-Next buttons cut off

### Root cause
The score drawer height is **fixed** at `UIScreen.main.bounds.height * 0.62` and the
content is wrapped in `.clipped()` (ScorecardView). The dial + header + buttons need a
fixed **~522pt**. On the ~844pt reference device `0.62 × 844 ≈ 523pt` — *just* fits, so it
passed dev testing. On any **shorter** phone `0.62 × H` drops **below** 522pt and
`.clipped()` silently severs the bottom (the Save / Score Next buttons).
→ **Device-height-dependent. That's why it shipped undetected.**

### Fix (design-preserving, per Daniel: "rewrite without changing the design at all")
Height becomes `max(0.62×H , intrinsicContentHeight + 36)`:
- Reference device: `0.62` term still wins → **pixel-identical**, nothing moves.
- Shorter devices: content floor wins → buttons (already part of the design) stop clipping.
- Device-independent → **Tyler's phone model no longer needed.**

### Files changed (uncommitted on `hotfix/1.1.2`)
| File | Change |
|---|---|
| `Carry/Views/ScoreInputSheet.swift` | + `static func minimumContentHeight(extraBottomPadding:)` — sums the fixed layout blocks (header 44+16, 2× spacer 24, dial 96×3, buttons 18×2+24, bottom 30+extra). Fonts are fixed-size (no Dynamic Type here) so it's exact per device. |
| `Carry/Views/ScorecardView.swift` | drawer `.frame(height:)` → `max(0.62×H + collapsed, minimumContentHeight(...) + 36)`. Comment explains the reference-device equivalence. |

Build: `xcodebuild … generic/platform=iOS Simulator` → **`** BUILD SUCCEEDED **`**, exit 0, 0 errors.

---

## BUG 1 — Duplicate guest users

### Symptom (Daniel)
Guests appeared **twice** when assembling tee groups — on his device AND the other scorer's.
Restarted the round, cleaned the dupes, **they came back**. Real Carry user (Keith, the
scorer) was NOT duplicated; only guests.

### Root cause — guests have TWO identities that never reconcile
`Player.id` is an `Int`. For a guest it is set two different ways:
- **Local create** (`addGuest`, PlayerGroupsSheet) → a local counter `nextGuestID` (100, 101, 102…).
- **Server load-back** (from `round_players`) → `Player.stableId(from: profileId)` = a hash of the UUID.

So the same person becomes `Kyle@100` (local) **and** `Kyle@<bighash>` (server-loaded).
Every roster-merge / dedup keys on `Player.id`:
- `QuickGameGuestStorage.save`: `seen.insert($0.id)` (Int)
- `GroupService` between-rounds backfill: `existingIds = Set(players.map(\.id))` (Int)
- `GroupManagerView` preservedGuests / source-4 merge: `freshIds`/`memberIds` (Int)

`100 ≠ bighash` → dedup misses → **both pills render.**
**Keith** is a real Carry user — always loaded from the server with the *same* id both
ways → dedups → no dupe. That's the tell: only guests double.

### Why it survives Restart Round (self-reinforcing loop)
The merge writes **both** copies back into `skins_groups.guest_roster_json` (durable
snapshot). Restart Round wipes the guest *profiles* but then **reads the poisoned
snapshot** → recreates two → dupes return. Same *shape* as the documented "Guest"+0.0
disease cycle (see `guest-lifecycle` skill, Disease-string rule).

### What still needs confirming before coding
Whether there are *also* genuinely duplicate **server profiles** (each scorer device minted
its own Kyle via `createGuestProfiles`) layered on top of the client loop.
Can't query prod directly (RLS-gated anon key; no service-role key locally).
→ Diagnostic: `docs/investigations/franks-payday-dupe-diagnostic.sql` (100% read-only).
Run **Query D** in Studio (project `seeitehizboxjbnccnyd` = Carry/Ohio = LIVE db).
- `distinct_player_ids = 1` per name → pure client/snapshot loop.
- `distinct_player_ids = 2` → server profiles also duplicated (fix needs server cleanup too).

### Proposed long-term fix (NOT yet implemented — Daniel: "best long-term fix")
Direction (pending Query D result + permission per "no patches" rule):
1. **One canonical identity:** guests keyed by `stableId(profileId)` everywhere; route ALL
   guest creation through `createGuestProfiles` so a server UUID exists *before* the Player
   gets an id (kills the local-counter divergence at the source).
2. **Dedup on `profileId`** (+ name/handicap safety key) across all 4 roster sources + the
   snapshot save path — not on the unstable Int id.
3. **Heal the snapshot on load:** collapse existing `guest_roster_json` dupes so already-poisoned
   groups self-clean.
Must preserve every guest-lifecycle locked invariant (guests in `round_players` only;
4-layer persistence chain; pills 1:1 contract; disease-string defenses).

---

## AUDIT — what we added, when, and why it broke

### Bug 2 provenance (real git blame)
| Commit | Date | What it added | Effect |
|---|---|---|---|
| `c235e51` | 2026-04-03 | "Handle no-show and incomplete groups in Quick Games" | introduced ALL THREE: `ScoreInputSheet`, the `0.62` fixed-fraction drawer height, AND `.clipped()` — in one commit |
| → shipped in | **1.1.0 (branch cut 2026-05-20, build 85)** | all present (grep-confirmed in the live binary) | latent on tall devices, clips on short |

**Why it broke:** the whole drawer landed at once in `c235e51`. The `0.62` fraction was
tuned on one tall reference device where it coincidentally ≈ content height. No
content-height floor → smaller screens clip. The `.clipped()` (added for the drag-dismiss
visual, 2 occurrences) turned an overflow into an invisible cut. Survived ~7 weeks because
the dev device was tall enough.

### Bug 1 provenance (real git blame)
| Commit | Date | What it added | Role in the bug |
|---|---|---|---|
| `a7e11b2` | 2026-03-05 | "Save current light theme state before dark theme restyle" | `nextGuestID = 100` local counter (**identity A**) |
| `c235e51` | 2026-04-03 | "Handle no-show and incomplete groups in Quick Games" | `Player.stableId(from:)` UUID→Int hash (**identity B**) — *same commit as Bug 2's drawer* |
| `10280c2` | 2026-05-10 | "hotfix/1.0.7: guest persistence, course-selection trap, push vault auth" | **THE TRIGGER** — dedup-by-Int-id between-rounds backfill + `QuickGameGuestStorage` Int-id dedup + `guest_roster_json` migration, all in this one commit |
| → shipped in | **1.1.0 (branch cut 2026-05-20, build 85)** | all present (grep-confirmed in the live binary) | dupes now visible + persistent |

**Why it broke:** dual identity (A from 2026-03-05, B from 2026-04-03) existed for
**~2 months** but was *latent* — nothing merged the two representations in a way the user
saw. The **`10280c2` (2026-05-10, hotfix/1.0.7)** guest-persistence work is the trigger: it
introduced (a) a roster-merge/backfill that deduped on the unstable Int id — making both
copies render — and (b) a durable `guest_roster_json` snapshot that captured both copies —
making the dupes survive Restart Round. **The very feature meant to *preserve* guests is
what made the latent identity split manifest and sticky.**

**Common thread:** both bugs are "tuned/works-on-my-setup" assumptions that hold on the
dev device/state and fail in the field — fixed-fraction height (one screen size) and
Int-id dedup (one creation path).

---

## DECISIONS LOCKED (from Daniel, this session)
| Question | Decision |
|---|---|
| Release path | **Pull 1.1.1, ship 1.1.2** (reject in-review build, fix both, submit build 88 as 1.1.2) |
| Bug 2 approach | **Rewrite without changing the design at all** (✅ done — `max()` keeps reference device identical) |
| Bug 1 depth | **Understand fully first, then best long-term fix** (no patch) |
| Live data cleanup | **Yes, clean it — but with my OK per step** (prod mutation gated) |

---

## RESUME CHECKLIST (if interrupted)
1. `git branch --show-current` should be `hotfix/1.1.2`. If not: `git checkout hotfix/1.1.2`.
2. Bug 2 edits are **uncommitted** in `ScoreInputSheet.swift` + `ScorecardView.swift`. Verify
   with `git diff release/1.1.0 -- Carry/Views/ScoreInputSheet.swift`.
3. Junk WIP is in `stash@{0}` — do NOT drop it.
4. Bug 1: waiting on Daniel's Query C/D output from the diagnostic SQL. Once received,
   finalize the canonical-identity fix + prep live cleanup (per-step OK).
5. Version bump still TODO: `MARKETING_VERSION 1.1.1 → 1.1.2`, `CURRENT_PROJECT_VERSION 87 → 88`.
6. Before any merge: confirm `junkEnabled` defaults OFF (it's not on this branch anyway).

## OPEN ITEMS
- [ ] Commit Bug 2 fix to `hotfix/1.1.2`
- [ ] Get Query C/D results → confirm Bug 1 server-profile dup or not
- [ ] Implement Bug 1 canonical-identity fix (with permission)
- [ ] Prep live-data cleanup steps for Frank's Payday (per-step OK)
- [ ] Version bump 1.1.2 / build 88
- [ ] Pull 1.1.1 from App Review
- [ ] Test both fixes on a short device (e.g. iPhone SE/13 mini) + 2-device guest flow
