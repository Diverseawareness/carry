# Test plan — 2026-05-01 fixes

Two production-impact fixes shipped today. This plan validates both before any App Store / TestFlight cut.

## What changed

### 1. Server: `notify_push()` per-table dispatcher
Migration: [supabase/migrations/20260501000000_fix_notify_push_per_table_dispatch.sql](../supabase/migrations/20260501000000_fix_notify_push_per_table_dispatch.sql)

- Rewrote `notify_push()` so every `NEW.<col>` reference lives inside a `TG_TABLE_NAME` IF block. Fixed the cross-binding 42703 that broke every Quick Game and Group rounds INSERT in TF 60.
- Re-attached `on_score_insert` trigger (dropped 2026-04-26 due to the same bug class).
- **Status:** APPLIED to prod via Supabase SQL Editor. No client change required — all live builds (1.0.1, 1.0.2, TF 60) start working immediately.

### 2. Client: Quick Game single-phase scorer_ids write
Files:
- [Carry/Models/SupabaseModels.swift](../Carry/Models/SupabaseModels.swift) — `SkinsGroupInsert.scorerIds` added.
- [Carry/Services/GroupService.swift](../Carry/Services/GroupService.swift) — `createGroup(... scorerIds:)` parameter.
- [Carry/Views/GroupsListView.swift](../Carry/Views/GroupsListView.swift) — `handleQuickGameCreate` computes `scorerIntIds` before `createGroup`, passes it in, follow-up `updateGroup(scorerIds:)` removed.
- [Carry/ViewModels/RoundViewModel.swift](../Carry/ViewModels/RoundViewModel.swift) — dead `canScore(holeNum:)` removed.
- [Carry/Views/ScorecardView.swift](../Carry/Views/ScorecardView.swift) — corresponding dead guard removed.

Fixes the Quick Game non-creator-scorer wedge: the `skins_groups` row is born with `scorer_ids` populated, so the invite push that fires from the same `createGroup` call delivers to clients that fetch a complete row. No more silent guard-5 trip on first tap.
**Status:** UNCOMMITTED, on `feature/auth-v2` working tree. Needs to land on `release/build-60` before next archive cut.

---

## Test matrix

### A. Server-side (no app build needed)
Run after the migration was applied — confirms current prod state still good.

**A.1 Trigger function shape**
```sql
SELECT length(prosrc), prosrc LIKE '%IF TG_TABLE_NAME = ''group_members'' THEN%' AS has_dispatcher
FROM pg_proc
WHERE proname = 'notify_push' AND pronamespace = 'public'::regnamespace;
```
Expect: one row, `has_dispatcher = true`, length ≈ 1595.

**A.2 All three triggers attached**
```sql
SELECT t.tgname, c.relname
FROM pg_trigger t
JOIN pg_class c ON c.oid = t.tgrelid
JOIN pg_namespace n ON n.oid = c.relnamespace
WHERE n.nspname = 'public' AND NOT t.tgisinternal
  AND t.tgname IN ('on_round_change', 'on_group_member_change', 'on_score_insert')
ORDER BY c.relname;
```
Expect: three rows — `on_group_member_change` on `group_members`, `on_round_change` on `rounds`, `on_score_insert` on `scores`.

### B. Quick Game end-to-end (post-build)
Pre-req: Build the app from `feature/auth-v2` (or whichever branch has the wedge fix), install on **two** devices.

**B.1 Quick Game starts cleanly (rounds INSERT)**
- Device A: tap Quick Start → pick a course → roster yourself + 1 guest, 1 tee group → Start Round.
- ✅ Active-round card appears on Home, scorecard opens, no red error toast.
- Verify in Supabase Logs (API Gateway, last 5 min): no 4xx on `POST /rest/v1/rounds`.

**B.2 Single-scorer scoring (you = scorer of group 1)**
- Continuing B.1: tap a hole, enter a score.
- ✅ Score saves. No 4xx on `POST /rest/v1/scores`.
- ✅ `on_score_insert` trigger doesn't roll back — confirmed via score row appearing in DB.

**B.3 Multi-group Quick Game with non-creator scorer (THE WEDGE TEST)**
- Device A: Quick Start → roster: yourself + 3 carry users + others, **2 tee groups**, person on Device B (e.g. Emese) is scorer of group 2.
- Device A: Start Round.
- Device B: receives "You're Invited!" / round-started push. Open the app **without force-quitting**.
- Device B: tap into the active-round card. Tap a hole to score.
- ✅ Score input appears on first tap. No silent wedge.
- ✅ No need to force-quit + reopen.

**Failure mode if regressed:** taps register no UI feedback. Console log on Device B should show `[Scorecard.tap] BLOCKED ...`. Paste the BLOCKED line if it surfaces.

**B.4 Score-dispute push (everyone-scores, edit existing score)**
- Continuing B.3 in a Skins Group with `scoring_mode='everyone'`: have Device A score hole 1 for player X, then Device B propose a different score for the same player/hole.
- ✅ Push fires to other group members.
- ✅ No 5xx in Supabase logs.

**B.5 All-groups-active creator push (was broken pre-fix)**
- 2-tee-group Quick Game from B.3.
- Device A scores at least one hole in group 1. Device B scores at least one hole in group 2.
- ✅ Creator (Device A) receives an "All groups are live" push when both groups have at least one score.
  This was a regression pre-fix (the trigger was dropped 2026-04-26). Phase 1 restored it.

### C. Round termination paths (smoke test)
Each path must NOT throw 4xx, and the round must transition to the right `status`.

**C.1 Skip Round** — RoundCompleteView Skip button.
- ✅ Round status flips appropriately (concluded or completed).
- ✅ No "Removed from group" alert fires for active members.

**C.2 Save Round Results** — RoundCompleteView Save.
- ✅ Round becomes `completed`, moves to Round History.

**C.3 End Game (destructive)** — Settings → End Game.
- ✅ Round becomes `cancelled`, scores deleted, push fans out.

**C.4 End & Save** — Settings → End & Save Results.
- ✅ Round becomes `concluded` with `force_completed=true`, push fans out, results sheet shows.

### D. Skins Group end-to-end (regression check)
**D.1 Group create + invite**
- Device A: create a Skins Group, invite Device B's Carry user.
- ✅ Push arrives on Device B with "You're Invited!".
- ✅ Tap-to-accept → status flips to `active`, banner shows "Member joined" on Device A.

**D.2 Group everyone-scores round**
- Start a round in the group from D.1.
- Both devices score holes.
- ✅ Live cross-device sync via realtime.
- ✅ No silent score wedge on either device's first tap.

### E. Identity / auth (smoke)
**E.1 Apple Sign In, fresh session**
- Sign out completely. Sign back in with Apple.
- ✅ Onboarding state correct (skip if already onboarded). Profile loaded.

**E.2 No Google sign-in regression**
- TF 60 / 1.0.2 release builds should NOT show Google button (auth-v2 is quarantined).

---

## F. Ephemeral guests — Phase 2 (server + iOS)

**Pre-reqs to apply (in order):**
1. [supabase/migrations/20260501000001_ephemeral_quick_game_guests.sql](../supabase/migrations/20260501000001_ephemeral_quick_game_guests.sql) — denormalize columns, drop FKs, add `delete_quick_game_guests` RPC.
2. [supabase/migrations/20260501000002_convert_quick_game_carry_only_auto_accept.sql](../supabase/migrations/20260501000002_convert_quick_game_carry_only_auto_accept.sql) — rewrite of `convert_quick_game_to_group` so Carry users stay `active` (no demotion to invited).
3. New iOS build with the Phase 2 iOS edits — DTO guest fields, `GuestProfileService.deleteQuickGameGuests`, RoundService termination hooks, wiped-guest fallback in `buildHomeRound` + `loadSingleGroup`.

These can be deployed together (the FK drop is forward-compatible — iOS DTO stayed non-optional, so old builds keep decoding fine). Migration #2 depends on #1 (calls the wipe RPC inside the convert RPC).

**F.1 New columns on round_players + scores**
```sql
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_schema = 'public'
  AND table_name IN ('round_players', 'scores')
  AND column_name IN ('player_id', 'guest_display_name', 'guest_handicap')
ORDER BY table_name, column_name;
```
Expect 6 rows. `player_id` stays `uuid NOT NULL` on both. `guest_display_name` `text YES`. `guest_handicap` `double precision YES`.

**F.2 FK constraints**
```sql
SELECT tc.table_name, tc.constraint_name, rc.delete_rule
FROM information_schema.table_constraints tc
JOIN information_schema.referential_constraints rc USING (constraint_schema, constraint_name)
WHERE tc.constraint_schema = 'public'
  AND tc.constraint_name IN (
      'round_players_player_id_fkey',
      'scores_player_id_fkey',
      'group_members_player_id_fkey'
  );
```
Expect 1 row only — `group_members_player_id_fkey` → `CASCADE`. The other two are dropped entirely (no FK to profiles).

**F.3 RPC exists and is grantable**
```sql
SELECT proname, pg_get_function_arguments(oid) AS args, prosecdef AS security_definer
FROM pg_proc
WHERE proname IN ('delete_quick_game_guests', 'convert_quick_game_to_group')
  AND pronamespace = 'public'::regnamespace;
```
Expect: two rows.

**F.4 Authorization gates**
- As a non-creator, call `SELECT delete_quick_game_guests('<some-other-creators-round-id>'::uuid);`
  ✅ Raises: `Not authorized — only the round creator can delete guests for round ...`
- Same with `convert_quick_game_to_group` for a non-creator group.

**F.5 Wipe round-trip end-to-end (build needs to be installed)**
- Device A (creator): create a Quick Game with you + 2 guests.
- `SELECT id, display_name FROM profiles WHERE created_by = '<your-uuid>' AND is_guest = true ORDER BY created_at DESC LIMIT 5;` — note the new guest UUIDs.
- Tap **Skip Round** on RoundCompleteView (or save / end / force-end — any termination).
- ✅ `SELECT count(*) FROM profiles WHERE id IN (<guest-uuids>);` returns 0.
- ✅ `SELECT player_id, guest_display_name, guest_handicap FROM round_players WHERE round_id = '<round-id>';` shows the original UUIDs preserved AND populated denormalize fields for the wiped guests.
- ✅ Round History on the creator's device still shows the round with the original guest names + handicaps.
- ✅ Per-hole scores in the historical scorecard still show under each guest (via UUID join, profile gone, render falls back to denormalize fields).

**F.6 Convert flow auto-accepts Carry users (Phase 3 collapsed in)**
- Device A: create a Quick Game with creator + 1 Carry user (Device B) + 1 guest. Play through.
- Device A: tap **Create Group** on RoundCompleteView. Phase-2 sheet opens.
- ✅ Device B does **not** receive a "You're Invited!" push (the demotion-to-`invited` was removed from `convert_quick_game_to_group`).
- ✅ Device B's HomeView shows the new Skins Group appearing on next refresh (no invite card, no opt-in step needed).
- ✅ The guest's profile row is deleted; the new Skins Group's `group_members` table contains creator + Carry user only (no guest row).
- ✅ Round History on both devices preserves the guest by name.

---

## G. Phase 5 — One-time legacy guest purge

**Pre-req:** Phase 2 (server + iOS) is in prod and validated via section F. Run only after a few real Quick Games have completed cleanly, so we know the wipe path works before applying it retroactively.

Script: [docs/phase-5-legacy-guest-purge.sql](phase-5-legacy-guest-purge.sql). Uses BEGIN…COMMIT so you can sanity-check before committing.

**G.1 Backfill + delete in one transaction**
- Open the script in Supabase SQL Editor.
- Run the first SELECT — note `legacy_guest_count` (should be a few hundred at most for v1.0.x).
- Run the two UPDATE statements (backfill round_players + scores).
- Run the DELETE.
- Run the three sanity-check SELECTs:
  - `remaining_guest_profiles` = 0
  - `orphaned_rp_with_no_name` = 0
  - `orphaned_scores_with_no_name` = 0
- ✅ All three return 0 → COMMIT.
- ❌ Any return > 0 → ROLLBACK + investigate.

**G.2 Round History smoke test post-purge**
- Open Round History on your device. Tap into an old round that had guests.
- ✅ Guests still show by name with correct handicap. Scores attribute correctly.

## H. Phase 6 — Cleanup the broken 12:20 group

Script: [docs/phase-6-cleanup-broken-1220-group.sql](phase-6-cleanup-broken-1220-group.sql).

**H.1**
- Run the sanity SELECT — confirm name='Fri, May 1', creator='339fb48e-…'.
- Run the round_players check — should be 0 (no round materialized).
- Run the two DELETEs.
- Verify `count = 0`.
- COMMIT.

✅ The dead group disappears from the Google test account's Games tab on next refresh.

---

Architectural rules locked in [quick_game_guest_lifecycle.md](../../.claude/projects/-Users-danielsigvardsson-Documents-Claude-carry/memory/quick_game_guest_lifecycle.md).

Phase 4 (strip `GuestProfileService.fetchMyGuests`) was done by side effect — the function had no callers in the codebase as of 2026-05-01 and was removed when `GuestProfileService` was edited to add `deleteQuickGameGuests`. Nothing left to verify there.

---

## I. 2026-05-02 follow-up fixes

Late-session work, all uncommitted on `feature/auth-v2`. Verification status per item:

| #    | Fix                                                                  | Status                                                                              |
|------|----------------------------------------------------------------------|-------------------------------------------------------------------------------------|
| I.1  | Quick Game Carry user → `active` on creation                         | ⏳ Needs rebuild test (just landed)                                                 |
| I.2  | Manage Members sheet auto-refresh                                    | ✅ Verified in-session (Emese flipped to active without force-quit)                  |
| I.3  | Remove-then-re-invite race fix                                       | ✅ Verified in-session ("worked" — Emese received the invite on the 2nd attempt)     |
| I.4  | X button on QuickStartSheet typing rows                              | ⏳ Needs rebuild test (visual)                                                      |
| I.5  | "Open your invite?" launch prompt gated on `skinGameGroups.isEmpty`  | ⏳ Needs rebuild test (multiple cold launches)                                      |
| I.6  | `loadSingleGroup` no longer leaks wiped guests + "Invited" label dropped on historical rows | ✅ Verified in-session (Ruby Hill Gc Skins shows only Daniel; leaderboard shows guests by name without misleading "Invited") |
| I.7  | Dead-code purge (guest-claim flow, ~150 lines / 8 files)             | ⏳ Needs Cmd+B verification (no dangling refs caught by compiler)                   |
| I.8  | "Set Tee Times to Start" button opens Game Options instead of starting round | ⏳ Needs rebuild test (just landed)                                          |
| I.9  | Pending Results sheet (regression check — view itself unchanged)     | ⏳ Spectator preview + real round with finished group + guest                       |
| I.10 | "Round Started" splash creator-only (explicit gate)                  | ⏳ Needs rebuild test (just landed) — defensive change, no user-visible behavior in current flows |
| I.11 | Scoring-info coachmark drops from 5 shows → 1 show                   | ⏳ Needs rebuild test                                                                |

Bare minimum to clear before cherry-picking to `release/build-60`: anything still ⏳ needs a quick rebuild + targeted test using the steps below.

### I.1 Quick Game — non-scorer Carry users now go in as `active` (not `invited`)
File: [Carry/Views/GroupsListView.swift](../Carry/Views/GroupsListView.swift) — `handleQuickGameCreate`, `scorerIdsToInvite` now gated on `scorerIntIdSet.contains(player.id)`.

Resolves the Keith/Ziggy "added Carry friend disappears from tee sheet" bug. Status reproduces identically across both names; root cause was the Carry user being persisted as `invited` and the tee-sheet filter (correctly) hiding invited members.

**Test cases:**

- **I.1.a Casual case (THE FIX)** — start a throwaway 1-group Quick Game, search a Carry friend, add to slot 1 or 2 (not the scorer slot). Save Quick Game. Open the new group.
  - ✅ Friend appears immediately in tee sheet.
  - ✅ Friend survives pull-to-refresh.
  - ✅ DB row: `status='active'`, `is_guest=false`.

- **I.1.b Scorer case (regression check — must still work)** — start a 2-group Quick Game; make a different Carry user (e.g. Emese) the scorer of group 2 (position 0 of group 2 in slot layout).
  - ✅ Emese's tile in the tee sheet shows the orange Pending pill until she accepts.
  - ✅ DB row: `status='invited'`, `is_guest=false`.
  - ✅ Emese receives the "You're Invited!" push (TF builds; dev builds will not push).
  - ✅ After Emese accepts, she flips to `active` and the Pending pill disappears.

- **I.1.c Guest case (regression check)** — type a random guest name in slot 1.
  - ✅ DB row: `status='active'`, `is_guest=true`.
  - ✅ Visible in tee sheet.

**SQL to verify all three at once for a fresh Quick Game:**
```sql
SELECT "sg"."id", "sg"."name", "gm"."status", "p"."display_name", "p"."is_guest"
FROM "skins_groups" "sg"
JOIN "group_members" "gm" ON "gm"."group_id" = "sg"."id"
LEFT JOIN "profiles" "p" ON "p"."id" = "gm"."player_id"
WHERE "sg"."created_at" >= CURRENT_DATE
ORDER BY "sg"."created_at" DESC, "gm"."status";
```

### I.2 Manage Members sheet — auto-refresh on open + pull-to-refresh
Files: [Carry/Views/ManageMembersSheet.swift](../Carry/Views/ManageMembersSheet.swift) (+`onRefresh` callback, `.task`, `.refreshable`), [Carry/Views/GroupManagerView.swift](../Carry/Views/GroupManagerView.swift) (passes `onRefresh: { await refreshGroupData() }`).

Resolves the "Test Friday" bug where Emese accepted an invite, server flipped her to `active`, but the creator's Manage Members sheet kept showing her as Pending until force-quit. Verified working in-session.

- **I.2.a** — invite a Carry user. While they're pending, open Manage Members on the creator side, leave the sheet open. Have them accept.
  - ✅ The 30s timer fires `refreshGroupData` → sheet's `localAllAvailable` updates → invitee moves from Pending to Playing without sheet dismiss/reopen.
- **I.2.b** — close + reopen Manage Members → `.task` fires → sheet shows current state immediately.
- **I.2.c** — pull down inside the sheet → `.refreshable` fires → fresh state.

### I.3 Manage Members — remove-then-re-invite race
File: [Carry/Views/ManageMembersSheet.swift](../Carry/Views/ManageMembersSheet.swift) — `inFlightRemovalTasks` tracking + Done button awaits before `onDone`.

Resolves the silent no-op when a user was long-press-removed and re-added in the same sheet session. Verified working in-session (Emese received the invite push on second test).

- **I.3.a** — open Manage Members → long-press a Carry member → confirm Remove → without dismissing, search the same person → tap Add → tap Done.
  - ✅ Done button briefly shows a `ProgressView` spinner while `removeMember` Task awaits.
  - ✅ Re-invited member receives the invite push (or sees the invite card on next Home refresh in dev builds).
  - ✅ DB shows their row recreated with `status='invited'` (not skipped silently).

### I.4 QuickStartSheet — X button on typing/guest rows
File: [Carry/Views/QuickStartSheet.swift](../Carry/Views/QuickStartSheet.swift)

- **I.4.a** — open Quick Start, type a guest name in any slot.
  - ✅ X (xmark.circle.fill) appears at the trailing edge of the slot.
  - ✅ Tap X → both `name` AND `handicap` fields clear in one tap.
- **I.4.b regression** — open Quick Start, leave a slot completely empty.
  - ✅ No X button shown (nothing to clear).
- **I.4.c regression** — search + tap a Carry user from the typeahead so the slot becomes a Carry-user row (avatar UI).
  - ✅ The X shown is the existing avatar-row X (line 939), NOT the new typing-row X. Both call `clearPlayerSlot` so behavior is identical, but they live in different code paths and shouldn't both render.
- **I.4.d regression** — type a name → tap X → confirm slot is fully reset (color slot pattern preserved per `clearPlayerSlot` logic).
  - ✅ Slot returns to empty state with the slot's default color seeded; nothing leaks from the prior content.

### I.5 HomeView — "Open your invite?" launch prompt gated on `skinGameGroups.isEmpty`
File: [Carry/Views/HomeView.swift](../Carry/Views/HomeView.swift) — `checkClipboardForInvite` early-returns if user has any groups.

- **I.5.a** — with at least one group on your device, kill + reopen the app several times.
  - ✅ "Open your invite?" alert never appears (regardless of clipboard contents).
- **I.5.b** — sign out, sign back in to a fresh user with zero groups, with any URL on the clipboard.
  - ✅ Alert still appears (post-install bridge still works).

### I.6 GroupManagerView — Tee Times / Manage Members guest pollution gone + leaderboard "Invited" label dropped on historical guests
Files: [Carry/Services/GroupService.swift](../Carry/Services/GroupService.swift) — `loadSingleGroup` no longer synthesizes wiped guests into the group's roster. [Carry/Views/GroupManagerView.swift](../Carry/Views/GroupManagerView.swift) — historical leaderboard row condition for "Invited" label dropped the `|| player.isGuest` clause.

Both verified clean in-session via the converted "Ruby Hill Gc Skins" group + the user's design call to keep guests off the cumulative leaderboard. Re-verify only if `loadSingleGroup` or the leaderboard row code is touched again.

- **I.6 regression check (only if touching above files)** — open a Skins Group that was converted from a Quick Game with guests.
  - ✅ Tee Times card shows only Carry members.
  - ✅ Manage Members → "Playing" + "All Members" both show only Carry members.
  - ✅ Group Leaderboard ("Last Round" + "All Time") shows guests by their denormalized name + handicap, NO "Invited" orange label, just like a regular player row.
  - ✅ Recent Games card on Home tab still shows guests by name (independent code path; this verifies it didn't regress).

### I.7 Dead-code cleanup (purge of guest-claim flow)
8 files touched, ~150 lines removed including `Carry/Views/GuestClaimView.swift` (whole file). See [quick_game_guest_lifecycle.md](../../.claude/projects/-Users-danielsigvardsson-Documents-Claude-carry/memory/quick_game_guest_lifecycle.md) for the full list.

- **I.7.a** — Cmd+B in Xcode after pulling these changes.
  - ✅ Build succeeds (no compile errors from dangling references).
- **I.7.b** — Debug Menu → confirm "Show Guest Claim Sheet" row is gone.
- **I.7.c** — Accept any group invite (Carry user → another Carry user's group). Confirm the join works without the auto-claim block in `acceptInvite`.

### I.8 GroupManagerView — "Set Tee Times to Start" button no longer silently starts the round
File: [Carry/Views/GroupManagerView.swift](../Carry/Views/GroupManagerView.swift) — start-button action gained an `else if needsTeeTimesSet { showSettings = true }` branch.

For Quick Games, `canStartRound` doesn't gate on `allTeeTimesSet`, so the start button is enabled even when the label switches to "Set Tee Times to Start". Pre-fix, tapping it fell through to the start-round fallback and unwillingly started the round. Now it opens Game Options (where tee times live).

- **I.8.a** — open a Quick Game with: 2+ players selected, course selected, `showTeeTimes` toggled ON, no tee times entered yet.
  - ✅ CTA reads "Set Tee Times to Start".
  - ✅ Tap → Game Options sheet opens (`showSettings = true`).
  - ✅ Round does NOT start (no `rounds` row created, no scorecard navigation, no Live Activity).
- **I.8.b** — set the tee times via Game Options, dismiss the sheet.
  - ✅ CTA flips to "Start Round".
  - ✅ Tap → round starts normally.
- **I.8.c regression** — Skins Group with no tee times set, 2+ players, course set.
  - ✅ Same behavior — button reads "Set Tee Times to Start", tap opens Game Options.
- **I.8.d regression** — `needsNextSchedule` (post-completed-round CTA "Schedule Next Round") still opens Game Options as before; the new branch is positioned AFTER `needsNextSchedule` so the existing branch fires first.

### I.9 Pending Results sheet — regression check after today's roster changes
File: [Carry/Views/ResultsSheet.swift](../Carry/Views/ResultsSheet.swift) — view itself didn't change today. Listed here because today's `loadSingleGroup` cleanup + `buildHomeRound` per-round wiped-guest synthesis split is the key load-bearing piece (Section I.6). If the split is wrong, Pending Results is one of the most visible places it would show.

A round enters "pending results" state when at least one tee group has finished all 18 holes but others are still scoring (`HomeRound.hasPendingResults` at HomeView.swift:93). Both spectators and finished players see it.

Debug entry point: Debug Menu → "Spectator: Pending Results" presents the sheet with `HomeRound.demoPendingResults` for visual review without playing.

- **I.9.a — debug spectator preview** — Debug Menu → Spectator: Pending Results.
  - ✅ Sheet renders. Pending winners + finished groups laid out cleanly. No empty state, no missing names.
- **I.9.b — real round with a guest in a tee group that's already done** — Quick Game with creator + 1 Carry user + 1 guest, 2 tee groups. Score group 1 to completion. Open the Pending Results sheet (tap the active card on Home).
  - ✅ The guest shows by name (denormalized via `guest_display_name`) with their handicap.
  - ✅ Pending winners section reflects group 1's results; group 2 shown as still scoring.
  - ✅ No "Invited" label or any guest-specific badge — guests render as regular per-round players in this view.
- **I.9.c — finished player view** — same setup as I.9.b. From the finishing scorer's device, view Pending Results.
  - ✅ Same layout as the spectator. Money totals reflect actual skins won so far. Pending skins counted with the "pending" indicator.

### I.10 RoundCoordinatorView — "Round Started" splash is now creator-only
File: [Carry/Views/RoundCoordinatorView.swift](../Carry/Views/RoundCoordinatorView.swift) — phase transition at line 222 area now reads `phase = isCreator ? .starting : .active`. The 5 staggered splash-animation `@State` writes are wrapped in `if isCreator { ... }` so non-creators don't burn cycles on an invisible view.

**Why it's defensive (not user-visible today):** in normal flows non-creators already skip the splash (they enter the coordinator with `initialRoundConfig != nil` → init forces `phase = .active`). The setup-tap-Start-Round path that sets `phase = .starting` was implicitly creator-only because the Start Round button is gated on `isCreator` in `GroupManagerView`. This change makes the gate explicit at the phase transition, so any future code path that lets a non-creator reach this code can't accidentally surface the splash.

- **I.10.a — creator path (regression check)** — set up a Skins Group, tap Start Round.
  - ✅ "Round Started" splash appears with flag pulse + staggered reveal.
  - ✅ Tap "Go to Scorecard" → scorecard.
  - ~~Tap "Back to Groups" → returns to setup~~ (button removed 2026-05-10 — UX trap; use scorecard `...` menu instead. See [phase-transitions.md](architecture/phase-transitions.md))
- **I.10.b — Quick Game with non-creator scorer of group 2** — Daniel (creator) starts a 2-tee-group Quick Game where Emese is scorer of group 2. Emese accepts the invite, taps the active round card on her Home tab.
  - ✅ Emese lands directly in the scorecard (no splash). This is the existing init-time `phase = .active` path; verifying it didn't regress.
- **I.10.c — anomalous non-creator path (the fix exercises this)** — only reachable if a future code path lets a non-creator tap Start Round in `GroupManagerView`. Hard to repro today; the in-DEBUG `print("[RoundCoordinator] currentUserId=\(currentUserId) creatorId=\(creatorId) isCreator=\(isCreator) ...")` line at RoundCoordinatorView:203 fires on every Start Round → confirm `isCreator=true` for creators, `isCreator=false` would route around the splash.

### I.12 PhoneInviteFinderSheet — auto-trigger on first Home appearance
File: [Carry/Views/PhoneInviteFinderSheet.swift](../Carry/Views/PhoneInviteFinderSheet.swift) + HomeView wiring + new RPCs in `supabase/migrations/20260502000001_phone_invite_finder.sql`.

Replaces the legacy clipboard "Open your invite?" alert + iOS "Allow Paste" double-prompt for the phone-invite flow. User types their phone (Apple AutoFill suggests it), server matches against pending `group_members.invited_phone` rows, user taps Join per match → reconciled. ZERO iOS clipboard prompts.

**Pre-req:** the migration must be deployed to prod (`SELECT public.find_pending_invites_by_phone('5551234567');` should return 0 rows without error).

- **I.12.a — fresh-install auto-trigger** — install the build via TF on a device that has no Carry data. From a SECOND device, send a phone-invite to the first device's phone number (e.g., from a Skins Group's Manage Members → invite by phone). The first device hasn't installed yet (you control timing).
  - Now install + open Carry on the first device.
  - Onboard.
  - On first Home appearance: ✅ "Find Your Invite" modal pops up automatically.
- **I.12.b — modal flow** — type the phone number → tap "Find Invites".
  - ✅ Server returns the pending invite, modal shows the group name + inviter.
  - ✅ Tap "Join" on the row.
  - ✅ Modal dismisses, brief progress, lands on Games tab with "You're in!" toast.
  - ✅ Group appears in Games list with you as `active` member.
- **I.12.c — modal "no results"** — same flow but type a phone number that doesn't match any pending invite.
  - ✅ Modal shows "No invites found for that number" + "Try a different number" CTA.
  - ✅ Skip dismisses cleanly.
- **I.12.d — Skip path** — modal appears, user taps "Skip" without searching.
  - ✅ Modal dismisses.
  - ✅ `hasSeenPhoneInviteFinder` is set, modal does NOT auto-trigger again on subsequent Home appearances.
- **I.12.e — never-invited regression** — install fresh, no SMS link tapped before install.
  - ✅ Modal does NOT auto-trigger (clipboard `hasURLs` returns false).

### I.13 Quick Game scorer SMS deep-link (Path C)
File: [Carry/Views/QuickStartSheet.swift](../Carry/Views/QuickStartSheet.swift) + [Carry/Views/ScorerAssignmentView.swift](../Carry/Views/ScorerAssignmentView.swift) + [Carry/Views/GroupsListView.swift](../Carry/Views/GroupsListView.swift) + the new `id` param on `GroupService.createGroup`.

Pre-allocates the Quick Game's UUID at slot-invite time so the SMS body embeds the per-group deep link from the very first send (no longer the broken generic `carryapp.site` link). Same UUID is then passed to `createGroup(id:)` on Continue so the server uses our pre-allocated id.

- **I.13.a — Quick Game scorer phone invite end-to-end** — start a Quick Game on Device A. Add yourself as scorer of group 1. Pick a course. Add a phone-invite scorer for group 2 (use Device B's phone number).
  - ✅ SMS composer opens immediately on Device A. Body reads: *"Score our skins game on Carry! https://carryapp.site/invite?group=<some-uuid>"* — verify the URL has a `?group=...` query param (NOT generic).
  - Tap Send → SMS arrives on Device B.
  - Continue setup, tap Continue on Device A.
  - Quick Game is created.
  - On Device B, tap the SMS link.
  - ✅ If Carry installed: opens via Universal Link → joins the group as `active` → toast "Joined". Same code path as QR scan.
  - ✅ If Carry not installed: invite.html opens → install Carry → onboarding → PhoneInviteFinderSheet auto-shows (per I.12.a) → enter Device B's phone → finds the invite → join.
- **I.13.b — UUID matches** — after the Quick Game is created on Device A, query the DB:
  ```sql
  SELECT id FROM skins_groups WHERE created_by = '<device A user UUID>' ORDER BY created_at DESC LIMIT 1;
  ```
  ✅ The id matches the UUID embedded in the SMS link from step I.13.a.

### I.14 Onboarding — new optional Phone Number step

File: [Carry/Views/OnboardingView.swift](../Carry/Views/OnboardingView.swift) + [Carry/Services/AuthService.swift](../Carry/Services/AuthService.swift) (now accepts `phone:` on `completeOnboarding`).

A new optional Phone step is inserted between Golf Profile and Notifications. Total step count is now 4 (Apple-name flow) or 5 (no-Apple-name flow). Skip is supported on the left button; right button reads "Next" and is always enabled. Entering a 10-digit number persists it digits-only on `profiles.phone` via `ProfileUpdate.phone`.

- **I.14.a — Apple Sign-In flow (4 steps)** — sign in with Apple on a fresh account that already has a name on the Apple ID. Progress bar shows 4 segments. Order: Golf Profile → **Phone (NEW)** → Notifications → Disclaimer.
  - ✅ Phone step shows: title "Your phone number", subtitle "Friends who invite you by phone…", a phone-pad TextField with `.textContentType(.telephoneNumber)` (iOS AutoFill suggests user's own number above the keyboard), and a small "Optional. We never share…" footnote.
  - ✅ Left button reads "Skip", right reads "Next" and is always enabled.
  - Tap Skip → advance to Notifications. Phone is NOT persisted.
  - Re-do the flow, this time type a 10-digit number → tap Next → finish onboarding.
  - In Supabase Studio, query: `SELECT phone FROM profiles WHERE id = '<user uuid>';` ✅ digits-only (no parens, no dashes).
- **I.14.b — No-Apple-name flow (5 steps)** — same as a, but starts with the Name step. Progress bar shows 5 segments. Order: Name → Golf Profile → **Phone (NEW)** → Notifications → Disclaimer. Skip behavior identical.
- **I.14.c — Auto-reconcile fires on first save** — pre-seed a row in `group_members` (status='invited', invited_phone='4155551234'). Sign up a new user with Apple Sign-In; on the Phone step, type that same number → Next → finish onboarding.
  - ✅ The pre-seeded `group_members` row's `player_id` flips to the new user, `invited_phone` clears, `status` becomes `active` (verify in Studio).
  - ✅ A `phoneInviteReconciled` push arrives on the new user's device with body "You've been added to <Group Name>!" within ~5 seconds.

### I.15 Phone-on-profile end-to-end (auto-reconcile + push)

Files: [supabase/migrations/20260502000002_phone_on_profile.sql](../supabase/migrations/20260502000002_phone_on_profile.sql) + Edge Function `send-push-notification` (`phoneInviteReconciled` branch) + [Carry/Views/ProfileSheetView.swift](../Carry/Views/ProfileSheetView.swift) (`PhoneEditSheet`) + [Carry/Views/HomeView.swift](../Carry/Views/HomeView.swift) (migration banner).

The server-side trigger `reconcile_phone_invites_for_profile` fires on `profiles.phone` insert/update. It auto-claims any pending phone-invite rows whose `invited_phone` matches the new value (created within last 30 days) and POSTs a push per claimed group to the recipient.

- **I.15.a — Settings update fires reconcile** — Device A signs into Carry with phone empty on profile. Device B (existing user, established group) creates a Quick Game and invites Device A's phone number from the scorer slot (Path C). On Device A: Profile → Phone Number row → enter Device A's number → Save.
  - ✅ Toast "Phone updated" on Device A.
  - ✅ A push notification arrives on Device A within ~5 seconds: "You've been added to <Group Name>!"
  - ✅ The group appears in the Games tab on Device A (status active).
  - ✅ In Studio, the matching `group_members` row shows `player_id = <device A user>`, `invited_phone = ''`, `status = 'active'`.
- **I.15.b — Migration banner for existing users** — Sign in on Device A as an existing user whose `profiles.phone` is null. On HomeView, ✅ a banner appears at the top of the scroll: "Add your phone — So friends can invite you to games" with an X to dismiss. Tap the banner → `PhoneEditSheet` opens. Tap Cancel → sheet closes. ✅ Banner stays visible. Re-tap → enter a number → Save → ✅ banner gone, toast shown.
- **I.15.c — Migration banner dismissal persists per user** — Tap the X on the banner → it disappears. Force-quit and relaunch ✅ banner stays gone. Sign out and sign in as a different (also phone-less) user → ✅ banner appears again (per-user dismissal key).
- **I.15.d — 30-day staleness guard** — Pre-seed an `invited_phone` row dated >30 days ago. New user sets matching phone → ✅ that row does NOT auto-claim (verify status still 'invited'). Recent rows still claim.
- **I.15.e — Clear phone from Settings** — Existing user with phone set: Profile → Phone Number → clear field → Save. ✅ Toast "Phone removed". ✅ Row in `profiles` shows `phone = ''`. No reconcile fires (trigger short-circuits on empty phone).
- **I.15.f — Phone formatting display** — In the Profile screen Account section, when `phone` is 10 digits, the row value renders `(415) 555-1234`. When unset, value reads "Add". Tap → sheet pre-fills the formatted version.

### I.11 ScorecardView — scoring-info coachmark shows once instead of 5 times
File: [Carry/Views/ScorecardView.swift](../Carry/Views/ScorecardView.swift) — threshold changed from `count < 5` to `count < 1`. Same UserDefaults key (`scoringInfoKeyPrefix + currentUserId`), so existing users who've already seen the modal 1–4 times will not see it again — the new threshold treats their stored count as already-shown. Fresh users see it exactly once.

- **I.11.a — fresh user** — sign in as a brand-new user, start a round, enter the scorecard at hole 1.
  - ✅ Scoring-info modal appears.
  - ✅ Tap dismiss. Exit + re-enter the scorecard (or start a new round on the same account). Modal does NOT re-appear.
- **I.11.b — existing user with partial count** — if any test account already has the key written 1–4 times pre-fix, no modal on subsequent rounds.
- **I.11.c — Quick Game non-scorer (regression check)** — scorer of group 1 (creator) and a non-scorer Carry user in the same Quick Game both open scorecard.
  - ✅ Scorer sees the coachmark (first time only).
  - ✅ Non-scorer never sees it (existing isCurrentUserScorerForOwnGroup gate at line 140 unchanged).
- **I.11.d — reset to re-test** — to manually re-trigger for re-testing on a single device, run in Xcode debugger console:
  ```
  expr UserDefaults.standard.removeObject(forKey: "<scoringInfoKeyPrefix>_<currentUserId>")
  ```
  Or sign out + sign back in as a different user.

---

## Pre-archive checklist (when ready to cut Build 61)

Beyond this test plan:

1. Trigger-fix migration committed on `release/build-60` (currently only on `feature/auth-v2`).
2. Wedge-fix client changes cherry-picked or merged into `release/build-60`.
3. `aps-environment` → `production` in `Carry.entitlements`.
4. `CURRENT_PROJECT_VERSION` bumped to 61.
5. `grantPremiumInTestFlight = false` in [StoreService.swift:44](../Carry/Services/StoreService.swift:44).
6. ASC stale screenshot updated (still shows "7 days free").
7. Verify ASC Introductory Offer is 30 days on both products.
8. Clean Build Folder → Archive → upload via App Store Connect.

(All other ASC items are listed in `MEMORY.md` "Pre-archive checklist".)
